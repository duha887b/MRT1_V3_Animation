#ifndef CONFIG_HH
#define CONFIG_HH

// Config Schnittstelle

#include "runtime_data.h"

// einlesen der settings.txt in runtime_data
void read_settings(list_header *list,char* path);

#endif //CONFIG_HH
